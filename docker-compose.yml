version: '3'
services:
  web:
    build: .
    ports:
      - "8888:8888"
    volumes:
      - ./misc/data:/app/misc/data

  # ngrok:
  #   image: ngrok/ngrok:latest
  #   restart: unless-stopped
  #   command: http web:8888
  #   environment:
  #     - NGROK_AUTHTOKEN=${NGROK_AUTHTOKEN}
  #   ports:
  #     - "4040:4040"
  #   depends_on:
  #     - web